distancia = float(input('Digite a distância em km a percorrer: '))
vm = float(input('Digite a velocidade média, em km/h, esperada para a viagem: '))

tempo = distancia / vm

print(f'O tempo de viagem será de {tempo:.1f} horas.')
